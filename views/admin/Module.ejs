<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('../partials/head.ejs') %>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous"/>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet"/>
        <link rel="stylesheet" href="/css/directorPannel.css" />
        <link rel="stylesheet" href="/public/css/forms.css" />
       
    </head>
    <body>
        <!-- Header -->
        <%- include('../partials/header.ejs') %>
        <!-- Header Ends!! -->
        <div class="container" id="header">
            <div>
                <h3>الفضاء الخاص بالإدارة</h3>
            </div>
            <div class="container" id="add-rem">
                <button class="btn" title="تسجيل الخروج">   
                    <a href="/logout" style="color: #6c757d">
                        <i id="bi-icon2" class="bi bi-box-arrow-left fw-bold"></i>
                    </a>
                </button>
                <button class="btn btn-primary my-5">
                    <a href="/add-Module" class="text-light">Add-Module
                        <i id="bi-icon" class="bi bi-plus fw-bold"></i>
                    </a>
                </button>
            </div>
        </div>

    <% if (Modules.length > 0) { %>
    <table class="table table-bordered w-75" id="center-container" dir="rtl">
        <thead class="thead-dark">
            <tr class="text-center">
                <!-- <th scope="col" style="cursor: pointer;" id="sort-id">&UpEquilibrium; رقم الاستاذ(ة)</th> -->
                <th scope="col">Nom du Module </th>
                <th scope="col">Description de Module</th>
                <th scope="col">Semestre du Module</th>
                <th scope="col">إجراء</th>
            </tr>
        </thead>
        <!-- Displaying the values -->
        <tbody class="table-group-divider">
            <% Modules.forEach(Module => { %>
                <tr class="text-center">
                    <td class="info"><%= Module.name_module  %></td>
                    <td class="info"><%= Module.desc_module %></td>
                    <td class="info"><%= Module.selected_semester %></td>
                    <td class="actions">
                        <button type="button" class="btn btn-warning">
                            <a class="fw-semibold text-decoration-none link-light py-3" href="/edit-Module/<%= Module._id %>">تحديث</a>
                        </button>
                        <form action="/edit-Module/<%= Module._id %>?_method=DELETE" method="POST" style="display: inline;">
                            <button type="submit" class="btn btn-danger">
                                <a class="fw-semibold text-decoration-none link-light py-3">حذف</a>
                            </button>
                        </form>
                    </td>
                </tr>
            <% }); %>
        </tbody>
    </table>
    <% } else { %>
        <div class="text-center mt-4">
            <p>.لا يوجد اي استاذ(ة)</p>
        </div>
    <% } %>

        <!-- footer -->
        <%- include('../partials/footer.ejs') %>
        <!-- footer ends -->
        
    </body>
</html>
