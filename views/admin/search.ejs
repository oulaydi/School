<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('../partials/head.ejs') %>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous"/>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet"/>
        <link rel="stylesheet" href="css/directorPannel.css" />
    
      
    </head>
    <body>
        <!-- Header -->
             <%- include('../partials/header.ejs') %>
        <!-- Header Ends!! -->
    
        <div class="container" id="header">
         
            
                
             
            <div>
                <h3>الفضاء الخاص بالإدارة</h3>
            </div>
         
            <div class="container" id="add-rem">
                <button class="btn" title="تسجيل الخروج">   
                    <a href="/logout" style="color: #6c757d">
                        <i id="bi-icon2" class="bi bi-box-arrow-left fw-bold"></i>
                    </a>
                </button>
              
                <button class="btn btn-primary my-5">
                    <a href="/admin/add-room" class="text-light">إضافة قاعة(ة)
                        <i id="bi-icon" class="bi bi-plus fw-bold"></i>
                    </a>
                </button>
             
            </div>


            <a href="/rooms" title="إلفاء" id="cancel" class="mt-5"><button type="button" class="btn-close" aria-label="Close" style="margin-top: 6px;"></button></a> 
           
        </div>
       
          
       
   
        <% if (arr.length > 0) { %>
          
    
          <div class="center-container">
      
            
            
            <div class="container my-5" dir="rtl">
              <table class="table table-bordered w-75" id="center-container" dir="rtl">
                <thead class="thead-dark">
                  <tr class="text-center">
                    <th scope="col">اسم القاعة(ة)</th>
                    <th scope="col">سعة القاعة</th>
                    <th scope="col">متوفرة</th>
                    <th scope="col">معدات القاعة</th>
                  </tr>
                </thead>
                <tbody class="table-group-divider">
                  <% arr.forEach(item => { %>
                    <tr class="text-center">
                      <td class="info"><%= item.name_room %></td>
                      <td class="info"><%= item.capacity_room %></td>
                      <td class="info"><%= item.selected_dispo_room %></td>
                      <td class="info"><%= item.equipement_room %></td>
                    </tr>
                  <% }); %>
                </tbody>
              </table>
            </div>
          <% } else { %>
            <div class="text-center mb-5">
              <p>.لا يوجد اي بببب(ة)</p>
            </div>
          <% } %>
        </div>
          
</body>
</html>